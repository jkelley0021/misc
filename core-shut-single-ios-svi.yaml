---


- name: rdc aci cutover
  hosts: 192.168.0.91


  tasks:


  - name: shut one svi at a time
    cli_config:
      config: "{{ lookup('template', 'templates/core-shut-svi.j2') }}"
    become: yes
    become_method: enable


...
